// ⚪ White — The Professional. Zero noise, brakes smoothly for the player, never swerves (SPEC §9.8).
// Everything about White lives in its profile; the hook is intentionally empty.
export const id = 'white';

export function tick() {}